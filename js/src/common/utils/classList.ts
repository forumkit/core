import clsx from 'clsx';

/**
 * 这个工具将 `clsx` 暴露给核心和扩展，作为一个可重用的工具。
 *
 * 要查看完整的文档，请访问 GitHub 上的 `clsx`
 *
 * @see https://github.com/lukeed/clsx
 */
const classList = clsx;

export default classList;
